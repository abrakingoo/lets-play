# lets-play
Let’s Play is a RESTful CRUD API built with Spring Boot and MongoDB. It manages users and products with full create, read, update, and delete operations. The project includes token-based authentication, role-based authorization, and follows backend best practices for secure, scalable API development.
